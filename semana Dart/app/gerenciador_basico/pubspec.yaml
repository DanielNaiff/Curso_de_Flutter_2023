name: grenciador_basico
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  # cupertino_icons: ^1.0.5
  # flutter_cupertino_date_picker: ^2.12.0
  firebase_auth: ^4.9.0
  firebase_core: ^2.15.1
  firebase_database: ^10.2.5
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  percent_indicator: ^4.2.3
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
